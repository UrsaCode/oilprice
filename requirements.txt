requests>=2.31
beautifulsoup4>=4.12
# IANA timezone database. Windows ships no system tz database, and minimal
# Linux images (e.g. alpine, slim) often omit it, so ZoneInfo("Asia/Karachi")
# raises ZoneInfoNotFoundError at import without this.
tzdata>=2024.1
# Reads the EU Weekly Oil Bulletin, published only as .xlsx.
openpyxl>=3.1
# Root store, combined with a bundled intermediate for bpc.gov.bd,
# which serves an incomplete certificate chain.
certifi>=2024.2
# PPAC publishes Indian daily pump prices only as PDF.
pypdf>=4.0
# opec.org decides what to serve from the TLS handshake and answers a plain
# client with an interstitial carrying a 200. curl_cffi shakes hands the way a
# browser does, so the public feed arrives as the feed. Used by one source
# only, through http.browser_get.
curl_cffi>=0.7
# That feed is remote XML, so it is not parsed with the standard library.
defusedxml>=0.7
